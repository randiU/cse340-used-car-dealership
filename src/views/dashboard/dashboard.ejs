<%- include("../partials/header") %>

<h1><%= title %></h1>

<main>
    <p>Welcome, <%= user ? user.firstName : "User" %>.</p>
    <p>Your role: <%= user ? user.roleName : "" %></p>
</main>

<%- include("../partials/footer") %>